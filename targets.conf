# Matriz de alvos suportados.
# Formato: target|dest_default|marker|min_version
# min_version: versao minima da ferramenta com a qual o alvo foi testado.
# CALIBRATION KNOB: dest_default e min_version devem ser confirmados contra a
# documentacao vigente de cada ferramenta (task T012). Valores abaixo sao os
# defaults iniciais documentados no plano.
claude|.claude/agents|.claude|1.0
codex|.codex/prompts|.codex|0.1
opencode|.opencode/agent|.opencode|0.1
