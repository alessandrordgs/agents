# Matriz de alvos suportados.
# Formato: target|dest_default|marker|min_version|ext
# min_version: versao minima da ferramenta com a qual o alvo foi testado.
# ext: extensao do artefato renderizado para o alvo.
# CALIBRATION KNOB: dest_default e min_version devem ser confirmados contra a
# documentacao vigente de cada ferramenta (task T012 da feature 001).
claude|.claude/agents|.claude|1.0|md
codex|.codex/agents|.codex|0.1|toml
opencode|.opencode/agent|.opencode|0.1|md
